name: Deploy Worker

on:
  push:
    branches:
      - main
      - claude/trading-bot-website-plan-2tjg13
    paths:
      - 'ayilon/worker-email.js'
      - 'ayilon/wrangler.toml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy ayilon-email worker
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Cloudflare Workers
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: ayilon
